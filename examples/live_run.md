# Результат сетевой проверки

Проверка выполнена **16.09.2026**, Python 3.12.14, SQLite 3.53.1.
Все три источника действительно загружены по HTTPS **без API-ключей**.
Это сетевой запуск, а не подстановка демо-данных.

```shell
python collect.py --timeout 30 --retries 1
python ioc.py --stats --format json
```

Итог: **Sources: ok=3 failed=0**, код завершения **0**.

| Источник | Уникальных IoC | Отклонено значений | Успешный сбор, UTC |
| --- | ---: | ---: | --- |
| Blocklist.de | 26,372 | 0 | 2026-09-16T19:07:14.260116Z |
| PhishTank | 75,877 | 35 | 2026-09-16T19:05:02.090292Z |
| ThreatView | 546,226 | 55 | 2026-09-16T19:06:38.614779Z |

В рабочей БД **642,376** уникальных IoC:
**6,099 High, 636,277 Medium, 0 Critical**.
High получен на реальных совпадениях IP ThreatView и Blocklist.de.
Сумма размеров источников больше размера БД из-за этих совпадений.

Типы: domain: 527,040, ipv4: 37,340, ipv6: 748, md5: 1,371, url: 75,877.

В PhishTank обработано 76 263 значения: 35 не прошли синтаксическую проверку,
351 повтор после нормализации. В ThreatView отклонены 2 значения IP и 53 домена.
Во всех файлах доля невалидных значений ниже порога 10%; остальные приняты.
Содержимое индикаторов не посещалось, файлы вредоносного ПО не скачивались.

Проверены целостность рабочей и демо-БД, внешние ключи и соответствие
`source_count` реальному числу связей. Статусы и времена сохранены в
[live_stats.json](live_stats.json). **60 тестов** пройдены без сети.

Демо: **11 IoC, 3 High, 8 Medium, 0 Critical** — [demo_run.txt](demo_run.txt).
Critical проверяется отдельно на трёх условных источниках: у выбранных
поставщиков нет общего для всех трёх импортируемого типа.

Состав выгрузок меняется. Размер списка не является оценкой покрытия БКС.
У PhishTank ограничено число анонимных скачиваний; загрузка большого списка
доменов ThreatView может занимать несколько минут. Для повторных проверок
логики предназначен `--demo`. Обновление файла не гарантирует актуальность
каждого отдельного IoC.
