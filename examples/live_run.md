# Результат сетевой проверки новой конфигурации

Проверка выполнена **16.09.2026**, Python 3.12.14, SQLite 3.53.1.
Успешный сбор PhishTank: **2026-09-16T18:42:28.496780Z**.

| Источник | Результат | Уникальных IoC |
| --- | --- | ---: |
| PhishTank | Успешная HTTPS-загрузка и обработка CSV | 75 877 |
| ThreatFox | Не задан ABUSECH_AUTH_KEY; запрос не отправлялся | 0 |
| MalwareBazaar | Не задан ABUSECH_AUTH_KEY; запрос не отправлялся | 0 |

Это **частичная сетевая проверка**, а не успешный запуск всех трёх источников.
Полный сетевой сбор ThreatFox и MalwareBazaar требует бесплатного ключа.
Их парсеры, параметры авторизации и ошибки проверены на синтетических данных;
реальные ответы авторизованных API в этом окружении не проверялись.

PhishTank: 76 263 значения, 75 877 уникальных URL, 351 повтор, 35 невалидных
значений. Загружались только данные фида, переходов по IoC не было.
Поставщик перенаправляет выгрузку на свой HTTPS CDN; этот переход разрешён явно.

Итог рабочей БД: **75 877 Medium, 0 High, 0 Critical**, тип — URL.
High отсутствует, поскольку пока успешно загружен только один источник.
Это не результат полного сравнения трёх наборов.
Статистика, время запросов и ошибки сохранены в [live_stats.json](live_stats.json).

Старая локальная БД v1 перед миграцией сохранена SQLite backup в
`data/iocs.sqlite3.v1-f69b5871.bak`. Записи прежних источников исключены из
рабочей БД; в резервной копии сохранены исходные данные.
Проверены целостность рабочей БД и резервной копии, внешние ключи и соответствие
сохранённого source_count реальному числу связей.

Без сети и ключей успешно пройдены **61 тест**. Демо даёт
**11 IoC: 3 High, 8 Medium, 0 Critical**:
[demo_run.txt](demo_run.txt). Уровень Critical отдельно проверен
на трёх условных источниках, без приписывания реальным сервисам чужих типов IoC.

При следующем сетевом запуске состав и размер выгрузки могут измениться.
